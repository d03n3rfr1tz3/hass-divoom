pytest==9.0.3
pytest-asyncio==1.4.0
pytest-homeassistant-custom-component==0.13.346
Pillow==12.2.0

# config_flow.py imports homeassistant.components.bluetooth, which in turn
# imports homeassistant.components.usb - both pull in these requirements at
# module level. Matches manifest.json's "bluetooth" trigger / "bluetooth_adapters"
# dependency, so this mirrors what a real HA instance installs for this integration.
aiousbwatcher==1.1.2
serialx==1.8.2
bleak==3.0.2
bleak-retry-connector==4.6.1
bluetooth-adapters==2.4.0
bluetooth-auto-recovery==1.6.4
bluetooth-data-tools==1.29.18
dbus-fast==5.0.22
habluetooth==6.26.5
